evenly divisible number


function lcm(n)
{
var answer=1;
for(var i=1;i<=n;i++)
answer=(answer*i)/(gcd(answer,i));
return answer;
}

function gcd(a,b)
{
if(a%b!=0)
return gcd(b,a%b);
else
return b;
}

console.log(lcm(20))
